# A股投研助手 0.2.6 自检报告

日期：2026-07-15  
环境：OpenClacky 1.4.0、Ruby 2.6、Node 22、Python 3.9.6

## 结论

扩展已按“用户从新建会话选择 A股投研助手，当前会话直接担任项目总控”的模式修复并安装。公开 Agent 目录中只暴露 `astock-research`；内部主席/委员只作为动态运行身份和规则模板，不再让用户选择。

项目代码、扩展注册、API、真实 HTTP 会话生命周期、浏览器首轮对话、面板加载以及 7 类真实数据包均已通过回归。未发现阻断启动或使用的项目代码错误。

## 自动化检查

| 检查 | 结果 |
|---|---|
| Ruby handler/lib 语法 | 通过 |
| Panel 全部 JavaScript 语法 | 通过 |
| Python bridge/vendor 编译 | 通过 |
| Shell 安装与 HTTP 测试脚本语法 | 通过 |
| `ext.yml` YAML 解析 | 通过 |
| OpenClacky `ext verify` | panel、API、唯一公开 Agent 全部 `[OK]` |
| Ruby API 集成测试 | 7/7 通过，覆盖 19 条路由、完整可变接口、并发硬限制、idle-running 自动续跑、完成报告误投自愈与最终报告完成门控 |
| Python 数据桥测试 | 5/5 通过，覆盖 7 类角色、签名映射、单源降级、mootdx F10、新版新浪财报结构 |
| 实机 HTTP 冒烟 | 通过：CRUD、日期校验、会话过滤、Worker 生命周期、停止/删除、保留总控会话并恢复目录 |
| Agent 目录 | 通过：存在 `astock-research`，不存在公开的 `astock-chair` / `astock-member` |
| 会话标题 | 通过：总控、初始 Worker、动态新增 Worker、恢复/重建统一使用“角色｜股票代码/任务” |
| 当前会话绑定 | 通过：面板挂载上下文缺失时从 `#session/<id>` 路由解析，创建项目复用当前 A股投研助手会话作为总控 |
| 启动并发控制 | 通过：启动时关联全部委员但保持 idle，仅唤醒总控；创建时可选 2 / 3 / 5，默认 3，`/progress` 与 `/message` 双重阻止超额派发 |
| 卡住自动恢复 | 通过：running 任务对应会话空闲超过 24 秒自动续跑；最多 3 次，超限后保持人工处理状态，不无限调用模型 |
| 完成报告路由 | 通过：Worker 误把“任务已完成”发给自己时，后端自动改投 Leader 并把对应任务标记 done |
| 最终交付门控 | 通过：16 个任务全部 done 后仍保持 running，只有根目录 `FINAL_REPORT.md` 非空才把项目标记 done；主席空闲且文件缺失时自动续跑收尾 |
| 固定任务名 | 通过：`PIPELINE.md` 任务名与 API `tasks[].name` 一致；兼容并规范化模型误加的“阶段 N ·”前缀，不再生成重复任务 |
| 中文消息编码 | 通过：模型生成的请求文本在唤醒和日志摘要前统一校正为有效 UTF-8，非法字节自动替换而不中断 Worker |
| 右栏滚动与负载 | 通过：轮询降至 8 秒，状态无变化时不重绘；确需刷新时保留主滚动容器及日志/任务区位置，用户滚动期间不替换 DOM |

现有运行项目 `601360` 的 1 个总控和 16 个后台角色会话已在不唤醒模型的情况下迁移标题，未重启任务、未增加模型调用。

## 浏览器实测

- 0.2.5 现场升级后，原科大讯飞项目准确复现“08 任务 running、数据质量官 idle、全项目无 Agent 运行”的假运行状态；看门狗在 24 秒宽限期后唤醒原 Worker，`any_running` 从 `false` 恢复为 `true`，没有创建重复会话。
- 原样重放数据质量官生成的错误完成消息（`worker_id` 与 `from` 都是它自己），后端自动改投 Leader，08 自动标记 done；随后主席自动并发启动 09/10，两者完成后又自动启动 11 研究裁决，项目始终保持 running。
- 科大讯飞现场继续无人值守推进：11 研究裁决 → 12 交易方案 → 13/14/15 三方风控（同时运行数恰为 3）→ 16 最终组合决策，16/16 任务全部 done。发现旧收尾逻辑会在最终文件生成前提前显示 done 后，0.2.5 门控把项目恢复为 running，并自动唤醒主席 1 次；最终写出 445 行、27,578 字节的 `FINAL_REPORT.md`，项目才转为 done，16/16 完成、0 Worker 错误。
- 0.2.5 面板显示“并发 3”。将右栏滚至 `900` 后跨过一次真实状态更新（总进度 8/16 → 9/16），滚动位置仍为 `900`；浏览器控制台 error 为 0。
- 创建表单实测包含 2 / 3 / 5 三档最大并发选项，默认选中“3 · 推荐”。

- 从“新建会话”选择 `A-Share Research Assistant / A股投研助手`并发送“你好”，成功创建会话。
- 首轮回复只检查当前工作目录的 `.clackyrules`，未搜索或接管旧的“新编排”目录。
- 回复明确引导用户在右侧“A股投研”面板填写代码并点击“创建并启动”。
- 面板成功显示 7 类分析师、快速/深度双模型、风险偏好和创建按钮。
- 针对真实案例补充回归：进入 `astock-research` 会话时右栏会自动选中“A股投研”，不再停留在通用“AI 团队”标签。
- 针对右栏回顶补充实机回归：将主滚动容器滚至 `1235`，等待 10.5 秒跨过至少两轮状态刷新后仍为 `1235`；再滚至中间位置 `775`，等待 8.5 秒后仍为 `775`。
- 完整创建科大讯飞 `002230` 实机项目 `research_14f3de9b66ad`：当前入口会话 `0c65168f1cd4cba4` 直接成为总控，16 个委员均创建独立会话，17/17 使用 `astock-research` profile、正确工作目录和“角色｜002230/任务”标题。
- 启动瞬间只有总控运行、16 个委员 idle；约 92 秒后总控自动并行派发 01–07，任务总数始终为 16，无重复任务、无并发上限和 UTF-8 错误。
- 01–07 自动生成 7 份 `REPORT.md` 和 7 份 `raw_data.md`，自动回传并标为 done；随后系统在无人操作下启动 08 数据质量门控。连续观察超过 5 分钟，项目始终为 running，没有因总控阶段性 idle 而停止。
- 扩展自身控制台 error 为 0；测试期间热重载触发的宿主 WebSocket 自动重连 warning 均已自行恢复。
- 浏览器测试会话已清理。用户原会话 `b4235f0b48f810f6` 保持原样，未删除、未改目录、未改配置。

## 真实数据回归（600519，截止 2026-07-15）

7/7 角色数据包均成功生成：`market`、`social`、`news`、`fundamentals`、`policy`、`hot_money`、`lockup`。

本轮同时修复了三类真实问题：

1. 股东信息函数多传日期参数；
2. 三张财务报表把日期误传为报表频率；
3. TradingAgents-Astock 0.2.18 未兼容 mootdx F10 字典返回和新浪 FinanceReport2022 新 JSON 结构。

修复后，K 线与 8 个技术指标、公司/宏观新闻、基本面、资产负债表、现金流量表、利润表、盈利预测、资金流、龙虎榜、解禁和股东信息均能形成数据段。

当前免费数据源仍有非阻断降级：财联社接口偶发返回非 JSON，程序会继续使用东方财富；东方财富行业横向比较接口本轮出现远端断连；mootdx K 线无返回时已成功回退新浪。这些情况会被明确写进数据包，不会中断其他来源或整个投研。

## 运行边界

- 不连接券商，不下真实订单；所有结论仅供研究。
- 删除投研项目只清理扩展创建的后台会话；用户创建的总控会话会保留并恢复原工作目录。
- 免费财经接口可能变化，数据层按来源独立降级，不能承诺每个外部字段永久可用。
